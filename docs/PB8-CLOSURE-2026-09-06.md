# Catalog Engine — PB8 Production Green Closure

Date: 2026-09-06
Slice: PB8 — Real Tenant Import
State: **PRODUCTION GREEN**

## Production proof

PB8 is closed only against the real first beta merchant, CROCCODILOS, using the isolated tenant data plane and the scheduler/Queue-owned initial import path.

Trusted proof target/main SHA:

`53752676fedd31f4924a9f2fa9a382f07cc921ce`

Trusted PB8 proof run:

`34045230609` — rerun completed successfully at 2026-09-06T17:11Z.

Proof result:

- `pb8ProductionProof = passed`;
- diagnosis `ready`;
- import `success / complete`;
- 6,104 discovered, 6,104 queued, 6,104 terminal details;
- detail terminal states: 6,097 success, 5 skipped, 2 deferred, 0 pending, 0 processing, 0 failed;
- isolated tenant catalog: 6,097 products and 15,396 media links;
- CEI/classification `success`: 6,097 products, 5,869 automatic, 228 review, 0 unknown;
- verification `success`: 6,097 products, 0 findings;
- scan Queue = 0;
- detail Queue = 0;
- scan DLQ = 0;
- detail DLQ = 0;
- private identifiers exposed = false;
- initial tenant import remains enabled;
- recurring Intelligent Sync remains disabled.

GitHub commit status on the exact proof SHA is now:

`catalog-engine/pb8-production-proof = success` — `Real isolated import + CEI + verification proven`.

## Root cause resolved during PB8

The final blocker was not catalog correctness. Terminal-success `finalize` DLQ residues were being rejected by cleanup logic that only admitted `detail` messages. The cleanup was corrected to recognize bounded, reference-scoped terminal `finalize` residues, D1 authority was revalidated, the residual DLQ was drained safely and the PB8 proof was rerun.

No recurring sync activation, M7E activation, public custom-domain publication, default-tenant fallback or private provider/runtime identifier exposure was introduced.

## Production runtime boundary

The application runtime remains the previously trusted deployed PB7/PB8-compatible runtime SHA:

`ccd69520607329acf764d3d5d29ddaaf29d0aa98`

The later main commits are proof/automation/documentation changes and do not require treating GitHub HEAD as an application Production SHA.

## Definition of Done

PB8's bounded contract is satisfied: a real newly created merchant tenant reached verified catalog readiness through the isolated production data plane, automatic initial scan/details/finalize, CEI classification and verification, with healthy Queue/DLQ state and recurring Intelligent Sync still OFF.

## Next approved slice

PB9 — Private Preview.

PB9 must introduce an authenticated preview that resolves tenant authority server-side, renders only the authorized merchant's effective catalog/brand/media, fails closed for anonymous and cross-tenant access, never falls back to the default tenant, does not expose supplier/runtime-private state and does not publish a permanent public merchant URL.
