# PB0 Closure — Portal Beta Live Truth + Sequencing Decision

Status: **GOVERNANCE GREEN / COMPLETE**  
Date: **2026-09-02**

## Scope closed

PB0 formalized the owner-authorized first real merchant beta sequencing exception without changing application/runtime code, Cloudflare resources, production data, tenant data or activation flags.

It established the bounded PB0–PB12 execution contract in `PORTAL-BETA-EXECUTION.md`, preserved M9B as incomplete/paused, aligned the focused business/portal/tenancy/SaaS contracts with an explicit server-side pilot entitlement grant, and reconciled stale pre-M5 Queue activation wording.

## Live baseline used

- pre-PB0 `main`: `3221c92945750596a3b52ae29dcb51bfcb687cea`;
- commit: `M9B: make club discovery copy truthful (#187)`;
- open PRs before PB0 branch: none;
- trusted application deploy `33537313254`: **SUCCESS** on the exact baseline SHA;
- the baseline's combined production regression statuses for Queue activation, fleet, affected-detail/CEI/verification/promotion, finalization, safe removal, automatic initial import and recovery/replay were all **SUCCESS**.

## Implementation / integration evidence

PB0 branch:

`agent/portal-beta-0-sequencing`

Validated PR head:

`c88ee4b66176873561d6814ace7bba4bd5970be0`

PR:

`#188 — PB0: formalize first real merchant beta sequencing`

PR validation on that exact head:

- `Validate SaaS control plane` run `33588925793` = **SUCCESS**;
  - dependency install = success;
  - repository quality gate = success;
  - clean control-plane migrations = success;
  - custom-domain provider-state verification = success;
  - isolated import/classify/verify job schema verification = success;
  - tenant-only schema application = success;
- `Validate tenant ingestion` run `33588925775` = **SUCCESS**;
  - dependency install = success;
  - repository quality gate = success;
  - staged scan/detail Worker config validation = success;
  - control-plane migrations = success;
  - current tenant data-plane schema verification = success.

PR `#188` was squash-merged from the exact tested head.

Integrated `main` SHA:

`75afe81880a856bb44493daf8b61e1578cd68451`

No competing PR or main movement occurred between the final anti-race check and merge.

## Why there is no PB0 Cloudflare canary

PB0 is documentation/governance-only. It changes no executable application/Worker asset, migration, Queue binding, D1 data, domain, secret or activation flag. The application deployment workflow is path-scoped to runtime/application inputs and there is no meaningful privileged Cloudflare mutation/canary to run solely for this sequencing decision.

Therefore the correct proof classification is **Governance Green**, not a new runtime Production Green claim. Production runtime remains the already-proven `#187` baseline until a behavior-changing PB slice is deployed.

## Decisions now authoritative for PB1+

1. PB1–PB12 names/order are formally approved.
2. M9B remains **IN PROGRESS / PAUSED**, not complete.
3. The normal public commercial path remains subscription/payment-driven.
4. The first beta may use an explicit, auditable, server-side pilot entitlement grant.
5. The beta grant must not be a hard-coded identity/email/provider-subject bypass.
6. Store creation must become entitlement-enforced server-side before PB3 enables it.
7. The default tenant cannot be reused for the beta.
8. Initial tenant import remains enabled through its proven Queue path.
9. Recurring Intelligent Sync remains disabled: automation `0`, active cohort empty, cap `1`.
10. M7E remains outside the campaign and requires separate owner authorization.
11. Supplier/private provider/runtime identifiers remain private.
12. Progress shown to merchants must be derived from durable real state.

## Important implementation gaps handed to PB1/PB2

PB1 begins from a real provider-neutral backend auth validator but no real merchant login UI/provider integration.

PB2 must close a pre-existing authorization gap: baseline `POST /api/admin/stores` can already persist a real tenant/profile/catalog instance/owner membership/provisioning run, but it does not yet evaluate an entitlement. Baseline `/api/admin/session` also does not return the entitlement projection already expected by the portal model.

## Rollback

If the owner revokes this sequencing exception before behavior-changing PB work lands, revert the PB0 planning integration and return execution to the preserved M9B point. No production tenant/runtime rollback is needed for PB0 itself because it changed no runtime state.

## Next approved slice

**PB1 — Authentication Foundation**

PB1 may begin only from live `main` revalidation and must preserve the OIDC/JWT provider-neutral boundary and no-password-storage rule.
