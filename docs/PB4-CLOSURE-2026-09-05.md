# PB4 Closure — Branding

Status: **PRODUCTION GREEN**  
Date: **2026-09-05**  
Campaign: first real merchant PB0–PB12  
Merchant proof tenant: **CROCCODILOS**

## Scope closed

PB4 is complete for the bounded first-merchant beta contract.

The production path now lets an authenticated merchant persist validated store identity, supported theme/colors, optional public contact fields and a normalized raster logo owned by the tenant. Logo bytes are stored in private Cloudflare R2 and public/profile state exposes only Catalog Engine-owned opaque asset identity/path.

No arbitrary merchant HTML, JavaScript or CSS is accepted. SVG upload remains disabled. No base64 logo blob is stored in D1 and no supplier-hosted logo dependency is used.

## Integrated runtime evidence

PB4 runtime was integrated on:

- main SHA `dfff6204e42a862c42cc091b70fc06243016e155`;
- PR #204 — branding/profile/validation experience;
- PR #205 — private R2 logo-storage recovery;
- trusted application deploy #127 / run `33952906777` — success on the exact runtime SHA;
- migration `0025` applied;
- private bucket `catalog-engine-brand-assets` provisioned by trusted deployment;
- Worker binding `BRAND_ASSETS` verified;
- Images binding retained for bounded image inspection/normalization;
- 145 test files / 714 tests green in deploy quality proof;
- production catalog smoke green;
- recurring Intelligent Sync remained disabled.

## First-real-merchant acceptance proof

The owner exercised the real CROCCODILOS branding flow in production after deploy #127:

```text
CROCCODILOS Appearance
-> choose valid raster logo
-> save identity
-> success confirmation rendered
-> leave/reload/re-enter portal
-> reopen Appearance
-> logo and saved branding state render again
```

The acceptance evidence shows the save succeeding and the same crocodile logo rendering again after re-entry/reload. This closes the production-only storage failure exposed by the earlier Cloudflare Images attempt and proves the private-R2 recovery works for the real beta merchant path.

The merchant-facing UI exposes the logo as Catalog Engine-owned branding state; private R2 object keys/provider locators are not surfaced in the customer-facing proof.

## Invariants preserved

- tenant ownership/isolation remains enforced server-side;
- unsupported/oversized/invalid image input fails closed;
- normalized public logo identity remains opaque;
- supplier/provider-private source state remains unrelated to branding;
- default tenant is not reused;
- `TENANT_IMPORT_AUTOMATION_ENABLED=1` remains allowed for initial import;
- `TENANT_SYNC_AUTOMATION_ENABLED=0` remains mandatory;
- active recurring-sync cohort remains empty;
- M7E remains outside the PB campaign.

## Rollback

If logo uploads become unhealthy, stop accepting new branding uploads and preserve the last valid profile/logo state. Existing tenant data must not be destructively removed merely to recover the upload path.

## Next approved slice

**PB5 — Source Connection**.

PB5 may now connect the merchant-facing portal to the existing authenticated source boundary for one private Yupoo source. It must preserve Provider Engine validation, membership/role authority, private source storage, destructive replacement guards and the recurring-sync-off invariant.
