# PB3 — Create Store Closure

Status: **PRODUCTION GREEN**  
Date: **2026-09-05**  
Repository: `lucasvenancio0110/catalog-engine`

## Scope closed

PB3 proved the first real merchant store-creation path end to end through the production customer portal.

The production path exercised:

```text
real Auth0 account
-> authenticated opaque principal
-> audited pilot entitlement
-> mobile create-store form
-> server-side entitlement gate
-> deterministic opaque tenant provisioning
-> owner membership/profile/catalog-instance/provisioning persistence
-> session reload
-> persisted store card + exhausted 1/1 beta allowance
```

The merchant created store **CROCCODILOS** through `app.catalogoengine.com`; after production reload, the portal returned the persisted store and displayed `1/1` allowance usage. The UI state came from `/api/admin/session`, not from a fabricated client object.

## Exact integrated production evidence

Final PB3 integrated production SHA:

`c42e9a5e2d67920678b998d64c6a0923546a7289`

Trusted application deploy:

- workflow: `Deploy Catalog Engine application`;
- run: `33947883746`;
- deployment number: **#125**;
- conclusion: **success**;
- D1 migrations: success;
- Worker/static application deploy: success;
- portal-auth binding verification: success;
- tenant import/sync boundary verification: success;
- application/catalog smoke: success.

Applicable production status contexts on the exact SHA were green, including application deploy, initial tenant import, tenant data-plane fleet, incremental/detail/CEI/verification/promotion/finalization/safe-removal and recovery/replay canaries.

## Production defects found and repaired during first-real-user proof

The real merchant proof found several gaps that unit-only PB3 coverage had not exposed. All were repaired before final acceptance:

1. portal deep-route asset base caused a blank Auth0 callback screen;
2. PB3 replay lookup referenced nonexistent `tenant_profiles` instead of `tenant_store_profiles`;
3. portal-to-control-plane create payload used `name` while the canonical planner requires `storeName`;
4. replay lookup selected nonexistent `catalog_tenants.currency` instead of `tenant_store_profiles.currency`.

Each repair was merged through bounded PR/CI and redeployed before the next production attempt. The final successful creation occurred only after the exact SHA above was deployed.

## Invariants preserved

- beta tenant is distinct from `t_00000000000000000001`;
- store creation remains server-entitlement-gated;
- one-store pilot allowance is enforced as `1/1` after creation;
- no hard-coded email/provider-subject authorization bypass;
- no customer password stored by Catalog Engine;
- no supplier URL/runtime/D1/Cloudflare locator exposed in the merchant create response;
- retries remain deterministic/idempotent;
- `TENANT_IMPORT_AUTOMATION_ENABLED=1` remains permitted;
- `TENANT_SYNC_AUTOMATION_ENABLED=0` remains mandatory;
- `TENANT_SYNC_ACTIVE_COHORT` remains empty;
- `TENANT_SYNC_MAX_JOBS_PER_TICK=1` remains unchanged;
- M7E remains unactivated.

## Definition of Done verdict

**PB3 — Create Store = PRODUCTION GREEN.**

A real beta merchant authenticated in production, received the bounded server-side entitlement, created one durable real store exclusively through the production control-plane mutation, and recovered the store through a later session reload.

## Next approved slice

**PB4 — Branding**

PB4 must implement validated tenant-owned branding/profile configuration and a safe tenant-logo asset pipeline without arbitrary merchant HTML/JavaScript/CSS, while preserving all tenant isolation and sync-off boundaries.
