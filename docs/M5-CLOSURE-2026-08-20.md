# M5 — Tenant Import / Queue Activation — Production Closure

Status: **COMPLETE**  
Date: **2026-08-20**  
Purpose: immutable-style evidence summary for the M5 Definition of Done. Operational truth continues in `CURRENT-STATE.md`; cross-tool continuation lives in root `CATALOG_ENGINE_HANDOFF_2026-08-20.md`.

## Definition of Done

M5 required:

```text
create tenant
→ connect supported source
→ isolated D1 import completes automatically
```

without one GitHub Action or manual Cloudflare operation per customer.

## Final production commit

`b917b023fde537baa0aa797d1230b7df7db5595e`

PR #75:

`m5: reactivate automatic import after canary Worker identity fix`

## Application deploy evidence

Workflow: `Deploy Catalog Engine application`  
Run: `32392783507`  
Status: `catalog-engine/application-deploy = success`

Verified:

- `TENANT_IMPORT_AUTOMATION_ENABLED="1"`;
- cron `*/5 * * * *`;
- scan/detail producers present;
- scan/detail consumers present;
- Worker version `a7923901-3463-44ac-b8f5-c4ba61804b9e`;
- 65 test files / 298 tests passed;
- 0 npm vulnerabilities;
- default catalog 17,018 products;
- 49,004 edge-proxy routes checked;
- supplier leak false;
- private state published false.

## Automatic canary evidence

Workflow: `Cloudflare automatic tenant import canary`  
Run: `32392875597`  
Job: `96502874428`  
Status: `catalog-engine/tenant-import-auto-canary = success`

Observed result:

```json
{
  "automaticTenantImportCanaryPassed": true,
  "automationEnabled": true,
  "manualQueueMessagesProduced": false,
  "schedulerDiscovered": true,
  "schedulerJobCreatedAt": "2026-08-20 16:40:40",
  "schedulerAttemptCount": 1,
  "discovered": 1,
  "completed": 1,
  "deferred": 0,
  "published": 1,
  "catalog": {
    "products": 1,
    "media": 2,
    "leaks": 0
  },
  "provisioning": {
    "runStatus": "running",
    "currentStep": "classify",
    "importStepStatus": "success",
    "importAttempts": 1
  },
  "defaultCatalogCountUnchanged": true,
  "queueBacklogsClean": true,
  "sourceScopeExpectedItems": 1
}
```

The canary itself did **not** send the initial Queue message. The five-minute production scheduler discovered the isolated tenant and drove the import.

## Additional M5 proofs retained

- real one/two-tenant Queue import: run `32338235562`;
- real retry -> DLQ -> repair -> replay: run `32338762195`;
- pre-activation read-only preflight: run `32339540357`;
- retained failed-canary diagnostic: run `32376786224`;
- targeted cleanup of failed fixture: run `32377942895`.

## Important defects fixed during activation

1. stale tests that incorrectly made ON/OFF irreversible;
2. scheduler/consumer race when a Queue message arrived while import job was still `pending`;
3. canary running before application deploy was fully complete;
4. automatic-canary Worker name `ce-auto-<suffix>` not matching production dispatch identity `ce-<suffix>`;
5. insufficient safe diagnostic visibility for import `jobErrorCode`.

## Operational invariants after closure

- automation ON is the production state at M5 closure;
- OFF remains a valid fail-safe rollback state;
- PR validation remains secret-free;
- no global Queue purge is acceptable as a normal recovery technique;
- historical OFF-only preflight/smoke contracts remain historical controlled proofs;
- canonical tenant Worker identity is `ce-<tenant suffix>`.

## Next milestone

**M6 — CEI Core + Sports Knowledge Pack v1**.

M6 should begin by auditing the existing sports classifier, normalized evidence, classification persistence/overrides and regression fixtures before introducing new abstractions.