# PB6 — Source Scope / Import Decision — Closure — 2026-09-06

Status: **PRODUCTION GREEN**

## Bounded outcome

PB6 is closed for the first-real-merchant Portal Beta campaign.

The real CROCCODILOS merchant explicitly confirmed the beta import decision through the authenticated production portal. The durable server-side decision is `full_connected_source`, its authority is `merchant`, it remains bound to the active private source, and the automatic initial-import discovery path consumed that authority without exposing the private supplier locator.

PB6 does not claim completion of the full real-tenant import journey. That remains PB8. PB7 is now the next approved slice and owns truthful merchant-facing provisioning/import progress.

## Final production implementation SHA

`5ec6f5e7159bfee6650da901f90d03ed16bf11f9`

This exact `main` SHA has both required production statuses green:

- `catalog-engine/application-deploy = success`;
- `catalog-engine/pb6-merchant-acceptance = success`.

## Implementation / recovery sequence

PB6 authority and acceptance were integrated through the bounded PR chain ending at the final exact production SHA:

- PR #212 introduced the durable provider-neutral import decision and migration `0026_tenant_import_decisions.sql`;
- PR #213 repaired mode-aware initial-import preflight validation;
- later PB6 proof work established real-merchant acceptance without projecting private identifiers;
- PR #223 moved fresh tenant physical D1/User Worker provisioning to trusted CI after production proved Worker-side Cloudflare administrative egress was exhausted/unreachable;
- PR #224 chained PB6 merchant acceptance proof to successful trusted provisioning and added a bounded scheduler wait without weakening the authority gate or enabling recurring sync.

The physical-provisioning recovery preserved the existing tenant, source connection and merchant decision. It did not recreate the store, replace the source, inject a Queue message manually or reset already-durable merchant state.

## Production defect found and corrected

The first real merchant flow exposed a production-only infrastructure issue before initial import could begin.

The CROCCODILOS fresh data-plane provisioning job exhausted six automatic attempts with the safe code `cloudflare_platform_unreachable`. The merchant decision itself was already durable and correct; the blocker was the physical creation path for the isolated D1/User Worker.

The corrected ownership boundary is:

```text
merchant decision / control-plane state
-> trusted GitHub Actions physical provisioning
-> isolated tenant D1 + User Worker
-> schema v8 verification
-> provisioning current_step=import
-> scheduler-owned initial-import discovery
```

Trusted fresh provisioning run `34036884111` completed successfully with safe evidence:

- one eligible fresh tenant selected;
- one advanced to `readyForImport`;
- zero failures;
- recurring Intelligent Sync remained disabled.

No private D1 UUID, Worker name, supplier URL, source locator, token or credential is recorded in this closure.

## Exact trusted-main deployment proof

Trusted application deploy run `34037208532` completed successfully for exact SHA `5ec6f5e7159bfee6650da901f90d03ed16bf11f9`.

The deploy proved, on the same SHA:

- repository quality gate;
- application build and artifact verification;
- D1 migrations;
- Worker/static application deployment;
- infrastructure and portal-auth binding verification;
- trusted tenant migration-capability preparation;
- tenant import producer and automation-boundary verification;
- deployed application smoke against the existing catalog;
- production status publication.

The resulting commit status is `catalog-engine/application-deploy = success` with description that app deploy, smoke, infrastructure bindings and the sync-off boundary were verified.

## Real merchant acceptance proof

Post-deploy PB6 merchant acceptance run `34037260274` completed successfully against the exact same SHA.

Safe production evidence showed:

- `pb6MerchantAcceptance = passed`;
- merchant = CROCCODILOS;
- decision kind = `full_connected_source`;
- authority = `merchant`;
- source binding proven;
- confirmation audit proven;
- initial import observed;
- initial import status had advanced to `details` during the final proof;
- initial-import automation enabled;
- recurring Intelligent Sync disabled;
- provisioning step = `import`;
- isolated catalog schema ready;
- isolated provider database ready;
- isolated provider Worker ready;
- supplier source active;
- active initial job present;
- physical data-plane job in success state.

The workflow published `catalog-engine/pb6-merchant-acceptance = success` with the bounded description `PB6 merchant authority, source binding and initial-import consumption proven`.

This satisfies the remaining gate defined by `PB6-PRODUCTION-PROOF-CHECKPOINT-2026-09-06.md`: a real authenticated CROCCODILOS merchant decision with durable `merchant` authority, same-source binding and proof that initial-import discovery consumed it.

## Privacy / isolation proof boundary

PB6 continues to preserve these invariants:

- supplier URL remains private;
- raw Yupoo/provider-private identifiers remain private;
- source locator remains server-side;
- D1/Worker/Cloudflare physical identifiers remain private;
- default tenant is not used as a fallback;
- the merchant decision is tenant/source-scoped;
- emitted proof uses only bounded merchant-safe state;
- no manual Queue injection was used to satisfy the acceptance gate.

## Activation boundary after closure

```text
INITIAL TENANT IMPORT = enabled
RECURRING TENANT INTELLIGENT SYNC = disabled
M7E = not activated
```

PB6 does not authorize recurring synchronization.

## What PB6 does not close

PB6 does not claim:

- completion of the CROCCODILOS initial import, CEI classification or verification — PB8 owns the end-to-end real-import proof;
- PB7 progress UX;
- authenticated private preview — PB9;
- Merchant Home — PB10;
- beta E2E — PB11;
- overall BETA GREEN — PB12;
- M7D11;
- M7E;
- M8;
- paused M9B or later M9 slices.

## Next approved slice

**PB7 — Provisioning Progress**.

PB7 must project truthful, resume-safe merchant progress from durable onboarding/import state with bounded polling and honest retry/error UX. It must not fabricate percentage progress or reinterpret PB6's successful import authority as full import completion.
