name: Cloudflare PB8 detail DLQ envelope classifier

on:
  pull_request:
    branches: ['main']
    paths:
      - '.github/workflows/cloudflare-pb8-dlq-envelope-classifier.yml'
      - 'scripts/cloudflare-pb8-dlq-envelope-classifier.mjs'
      - 'tests/pb8-dlq-envelope-classifier.test.mjs'
  push:
    branches: ['main']
    paths:
      - '.github/workflows/cloudflare-pb8-dlq-envelope-classifier.yml'
      - 'scripts/cloudflare-pb8-dlq-envelope-classifier.mjs'
      - 'tests/pb8-dlq-envelope-classifier.test.mjs'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx vitest run tests/pb8-dlq-envelope-classifier.test.mjs
      - run: npm run quality

  diagnose:
    if: >-
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')
    runs-on: ubuntu-latest
    timeout-minutes: 12
    env:
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
    steps:
      - name: Checkout trusted main
        uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx vitest run tests/pb8-dlq-envelope-classifier.test.mjs
      - name: Validate trusted Cloudflare credentials
        shell: bash
        run: |
          set -euo pipefail
          test -n "$CLOUDFLARE_ACCOUNT_ID"
          test -n "$CLOUDFLARE_API_TOKEN"
      - name: Classify safe aggregate envelope structure without consuming messages
        shell: bash
        run: |
          set -euo pipefail
          node scripts/cloudflare-pb8-dlq-envelope-classifier.mjs | tee /tmp/pb8-dlq-envelope-classifier.json
          cat /tmp/pb8-dlq-envelope-classifier.json >> "$GITHUB_STEP_SUMMARY"
