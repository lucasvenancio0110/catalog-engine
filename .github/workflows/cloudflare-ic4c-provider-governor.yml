name: Cloudflare IC4C adaptive provider governor proof

on:
  pull_request:
    branches: ['main']
    paths:
      - '.github/workflows/cloudflare-ic4c-provider-governor.yml'
      - 'tests/ic4c-provider-governor-proof.test.mjs'
      - 'tests/ic4c-provider-detail-governor.test.mjs'
      - 'worker/ic4c-governor-probe.js'
      - 'worker/import-detail-entry.js'
      - 'worker/ingestion/provider-detail-governor.js'
      - 'wrangler.import-detail.jsonc'
  workflow_run:
    workflows: ['Deploy Catalog Engine application']
    types: [completed]
  workflow_dispatch:

permissions:
  contents: read
  statuses: write

jobs:
  validate:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 18
    steps:
      - name: Checkout PR head
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify IC4C governor and proof contracts
        run: >-
          npx vitest run
          tests/ic4c-provider-detail-governor.test.mjs
          tests/ic4c-provider-governor-proof.test.mjs
          tests/tenant-import-detail.test.mjs
          tests/tenant-import-queue-config.test.mjs
          tests/tenant-data-plane-command.test.mjs
          tests/tenant-m7d10-recovery-canary.test.mjs
          tests/exact-sha-production-proof-trigger-alignment.test.mjs

      - name: Secret-free quality gate
        run: npm run quality

  prerequisites:
    if: >-
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'workflow_run' &&
       github.event.workflow_run.conclusion == 'success' &&
       github.event.workflow_run.head_branch == 'main')
    runs-on: ubuntu-latest
    timeout-minutes: 55
    outputs:
      sha: ${{ steps.target.outputs.sha }}
    steps:
      - name: Resolve exact deployed trusted-main SHA
        id: target
        env:
          EVENT_SHA: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || github.sha }}
        shell: bash
        run: |
          set -euo pipefail
          test -n "$EVENT_SHA" || { echo 'Missing target SHA'; exit 1; }
          echo "sha=$EVENT_SHA" >> "$GITHUB_OUTPUT"

      - name: Wait for exact-SHA deploy, Queue, provider, IC4B and LKG regressions
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          SHA: ${{ steps.target.outputs.sha }}
        shell: bash
        run: |
          set -euo pipefail
          for attempt in $(seq 1 300); do
            STATUS_JSON="$(curl --fail --silent --show-error \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H 'Accept: application/vnd.github+json' \
              -H 'X-GitHub-Api-Version: 2022-11-28' \
              "https://api.github.com/repos/$REPO/commits/$SHA/status")"
            APP_STATE="$(printf '%s' "$STATUS_JSON" | jq -r '[.statuses[] | select(.context=="catalog-engine/application-deploy")][0].state // "missing"')"
            QUEUE_STATE="$(printf '%s' "$STATUS_JSON" | jq -r '[.statuses[] | select(.context=="catalog-engine/queue-consumer-activation")][0].state // "missing"')"
            AUTO_STATE="$(printf '%s' "$STATUS_JSON" | jq -r '[.statuses[] | select(.context=="catalog-engine/tenant-import-auto-canary")][0].state // "missing"')"
            PB9_STATE="$(printf '%s' "$STATUS_JSON" | jq -r '[.statuses[] | select(.context=="catalog-engine/pb9-production-proof")][0].state // "missing"')"
            IC2_STATE="$(printf '%s' "$STATUS_JSON" | jq -r '[.statuses[] | select(.context=="catalog-engine/ic2-production-proof")][0].state // "missing"')"
            IC3_STATE="$(printf '%s' "$STATUS_JSON" | jq -r '[.statuses[] | select(.context=="catalog-engine/ic3-production-proof")][0].state // "missing"')"
            IC4B_STATE="$(printf '%s' "$STATUS_JSON" | jq -r '[.statuses[] | select(.context=="catalog-engine/ic4b-detail-fanout")][0].state // "missing"')"
            if [ "$APP_STATE" = success ] && [ "$QUEUE_STATE" = success ] && \
               [ "$AUTO_STATE" = success ] && [ "$PB9_STATE" = success ] && \
               [ "$IC2_STATE" = success ] && [ "$IC3_STATE" = success ] && \
               [ "$IC4B_STATE" = success ]; then
              echo 'exact_sha_application_deploy=success'
              echo 'exact_sha_queue_activation=success'
              echo 'exact_sha_real_provider_canary=success'
              echo 'exact_sha_pb9_lkg=success'
              echo 'exact_sha_ic2_isolation=success'
              echo 'exact_sha_ic3_listing=success'
              echo 'exact_sha_ic4b_fanout=success'
              exit 0
            fi
            case "$APP_STATE:$QUEUE_STATE:$AUTO_STATE:$PB9_STATE:$IC2_STATE:$IC3_STATE:$IC4B_STATE" in
              *failure*|*error*)
                echo "Exact-SHA dependency failed: app=$APP_STATE queue=$QUEUE_STATE auto=$AUTO_STATE pb9=$PB9_STATE ic2=$IC2_STATE ic3=$IC3_STATE ic4b=$IC4B_STATE"
                exit 1
                ;;
            esac
            sleep 10
          done
          echo "Exact-SHA dependencies unresolved: app=$APP_STATE queue=$QUEUE_STATE auto=$AUTO_STATE pb9=$PB9_STATE ic2=$IC2_STATE ic3=$IC3_STATE ic4b=$IC4B_STATE"
          exit 1

  prove:
    needs: prerequisites
    if: needs.prerequisites.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 22
    env:
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      TARGET_SHA: ${{ needs.prerequisites.outputs.sha }}
      WRANGLER_VERSION: 4.123.0
      STATUS_CONTEXT: catalog-engine/ic4c-provider-governor
      WORKERS_SUBDOMAIN: lucassantanals0110

    steps:
      - name: Checkout exact deployed trusted-main SHA
        uses: actions/checkout@v4
        with:
          ref: ${{ env.TARGET_SHA }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Validate trusted Cloudflare credentials
        shell: bash
        run: |
          set -euo pipefail
          test -n "$CLOUDFLARE_ACCOUNT_ID" || { echo 'CLOUDFLARE_ACCOUNT_ID is missing'; exit 1; }
          test -n "$CLOUDFLARE_API_TOKEN" || { echo 'CLOUDFLARE_API_TOKEN is missing'; exit 1; }

      - name: Install dependencies
        run: npm ci

      - name: Quality and IC4C contract gate before proof resource
        run: |
          npx vitest run tests/ic4c-provider-detail-governor.test.mjs tests/ic4c-provider-governor-proof.test.mjs tests/tenant-import-detail.test.mjs tests/tenant-data-plane-command.test.mjs
          npm run quality

      - name: Create isolated IC4C Durable Object probe config
        id: probe
        shell: bash
        run: |
          set -euo pipefail
          PROBE_WORKER_NAME="ce-ic4c-proof-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          PROBE_TOKEN="$(node -e "process.stdout.write(require('crypto').randomBytes(32).toString('hex'))")"
          PROBE_CONFIG="$(mktemp -p "$RUNNER_TEMP" ic4c-governor-XXXXXX.json)"
          node --input-type=module - "$PROBE_CONFIG" "$PROBE_WORKER_NAME" "$PROBE_TOKEN" <<'NODE'
          import fs from 'node:fs';
          const [configPath, workerName, token] = process.argv.slice(2);
          fs.writeFileSync(configPath, JSON.stringify({
            name: workerName,
            main: './worker/ic4c-governor-probe.js',
            compatibility_date: '2026-08-17',
            workers_dev: true,
            vars: { PROBE_TOKEN: token },
            durable_objects: {
              bindings: [{ name: 'PROVIDER_DETAIL_GOVERNOR', class_name: 'ProviderDetailGovernor' }]
            },
            migrations: [{ tag: 'ic4c-proof-v1', new_sqlite_classes: ['ProviderDetailGovernor'] }]
          }, null, 2));
          NODE
          echo "worker_name=$PROBE_WORKER_NAME" >> "$GITHUB_OUTPUT"
          echo "config=$PROBE_CONFIG" >> "$GITHUB_OUTPUT"
          echo "token=$PROBE_TOKEN" >> "$GITHUB_OUTPUT"

      - name: Deploy isolated guarded IC4C Durable Object probe
        shell: bash
        run: |
          set -euo pipefail
          if ! npx --yes "wrangler@$WRANGLER_VERSION" deploy --config "${{ steps.probe.outputs.config }}" >/tmp/ic4c-probe-deploy.log 2>&1; then
            echo 'IC4C isolated probe deploy failed; raw resource identifiers remain suppressed.'
            exit 1
          fi
          echo 'IC4C isolated guarded Durable Object probe deployed.'

      - name: Prove coordinated AIMD, hard ceiling and pressure slowdown
        id: evidence
        shell: bash
        run: |
          set -euo pipefail
          PROBE_URL="https://${{ steps.probe.outputs.worker_name }}.${WORKERS_SUBDOMAIN}.workers.dev/run"
          ok=0
          for attempt in $(seq 1 30); do
            CODE="$(curl --silent --output /tmp/ic4c-governor-evidence-safe.json --write-out '%{http_code}' \
              -X POST -H 'cache-control: no-store' \
              -H "x-probe-token: ${{ steps.probe.outputs.token }}" \
              "$PROBE_URL" || true)"
            if [ "$CODE" = 200 ]; then ok=1; break; fi
            sleep 2
          done
          test "$ok" = 1 || { echo 'IC4C guarded probe did not become ready.'; exit 1; }
          jq -e '
            .ic4cAdaptiveGovernor == "passed" and
            .contractVersion == 1 and
            .durableObjectCoordination == true and
            .initialLimit == 2 and
            .firstHealthyWindowLimit == 3 and
            .secondHealthyWindowLimit == 4 and
            .hardCeiling == 4 and
            .concurrentAttempted == 8 and
            .concurrentAdmitted == 4 and
            .concurrentRejected == 4 and
            .pressure.throttled429.reducedLimit == 2 and
            .pressure.throttled429.cooldownRejected == true and
            .pressure.upstream5xx.reducedLimit == 2 and
            .pressure.upstream5xx.cooldownRejected == true and
            .pressure.timeout.reducedLimit == 2 and
            .pressure.timeout.cooldownRejected == true and
            .pressure.degradedLatency.reducedLimit == 2 and
            .pressure.degradedLatency.cooldownRejected == true and
            .recurringIntelligentSyncChanged == false and
            .privateIdentifiersExposed == false
          ' /tmp/ic4c-governor-evidence-safe.json >/dev/null
          cat /tmp/ic4c-governor-evidence-safe.json >> "$GITHUB_STEP_SUMMARY"

      - name: Delete isolated IC4C proof Worker and Durable Object namespace
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          rm -f "${{ steps.probe.outputs.config }}"
          HTTP_CODE="$(curl --silent --output /tmp/ic4c-delete.json --write-out '%{http_code}' \
            -X DELETE \
            -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
            -H 'Accept: application/json' \
            "https://api.cloudflare.com/client/v4/accounts/$CLOUDFLARE_ACCOUNT_ID/workers/scripts/${{ steps.probe.outputs.worker_name }}" || true)"
          test "$HTTP_CODE" = 200 || test "$HTTP_CODE" = 404 || {
            echo 'IC4C isolated proof resource cleanup failed.'
            exit 1
          }
          echo 'IC4C isolated proof resource cleanup complete.'

      - name: Publish successful IC4C production proof
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: |
          set -euo pipefail
          jq -n --arg state success --arg context "$STATUS_CONTEXT" \
            --arg description 'IC4C green; DO AIMD 2->4; global ceiling=4; pressure slowdown proven' \
            --arg target_url "$RUN_URL" \
            '{state:$state,context:$context,description:$description,target_url:$target_url}' > /tmp/status.json
          curl --fail --silent --show-error -X POST \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H 'Accept: application/vnd.github+json' \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/repos/$REPO/statuses/$TARGET_SHA" -d @/tmp/status.json

      - name: Publish failed IC4C production proof
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: |
          set -euo pipefail
          test -n "$TARGET_SHA" || exit 0
          jq -n --arg state failure --arg context "$STATUS_CONTEXT" \
            --arg description 'IC4C trusted adaptive-governor proof failed; IC4B topology remains rollback authority' \
            --arg target_url "$RUN_URL" \
            '{state:$state,context:$context,description:$description,target_url:$target_url}' > /tmp/status.json
          curl --fail --silent --show-error -X POST \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H 'Accept: application/vnd.github+json' \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/repos/$REPO/statuses/$TARGET_SHA" -d @/tmp/status.json
